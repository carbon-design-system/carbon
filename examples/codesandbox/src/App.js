import { Button } from '@carbon/react';

/**
 * To reduce start-up time, this codesandbox is configured to use precompiled style assets (css).
 * A separate codesandbox is available with sass compilation configured:
 * https://codesandbox.io/s/github/carbon-design-system/carbon/tree/main/examples/codesandbox-with-sass-compilation/
 */

function App() {
  return (
    <div>
      <Button>Hello world</Button>
    </div>
  );
}

export default App;
