<nav class="bx--side-nav__nav">
  <ul class="bx--side-nav__nav-items">
    {{#if sidenav.state.hasIcons}}
      {{> platform-side-nav-category sidenav=sidenav}}
    {{else}}
      {{> platform-side-nav-category-fixed sidenav=sidenav}}
    {{/if}}
  </ul>
</nav