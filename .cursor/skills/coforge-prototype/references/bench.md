# Prototype bench

Skipped ≠ Pass. Human Gate A — do not self-approve.

| Check                                                                  | Pass                     |
| ---------------------------------------------------------------------- | ------------------------ |
| Every proto-spec hotspot exists (Figma reaction **and** React control) |                          |
| Every `forbidden_hotspots` absent on both surfaces                     |                          |
| P09 no Book / checkout / hold                                          |                          |
| P14 wrong vs right pair distinct by layout + label, not colour-only    |                          |
| P04 share: no recipient account                                        |                          |
| P12: sequence is product; no “simplified UI” onboarding                |                          |
| No sitewide search                                                     |                          |
| Entry `/` and one exit reachable                                       |                          |
| Empty + one error (or blocked-wrong-document) per shipped flow         |                          |
| Keyboard: Header, labelled Search, tiles, links (React)                |                          |
| Colour overlay rows for used tokens still Pass                         |                          |
| Figma IBM-blue link                                                    | Skip-with-reason allowed |

Evidence: screenshot or URL + node id / route. Write `product/VERIFY.md` or
`screens/<slug>/VERIFY.md`.
