# Bans

- Git: not `Coforge_skin` → stop. Do not merge to `main` unless asked.
- Figma writes to published main `Ude8f8dEgXxxnpbzrvWfwE`. Writes only
  `mnPFHuLUzXItWQimrWQEvV`.
- Edit `packages/themes/src/dtcg/themes.json` or Style Dictionary `THEME_NAMES`.
- Prefix `Button` / `cds-button` with CoForge.
- Skin `g90` / `g100`.
- Remap `layer-01/02/03`, `field-*`, `border-subtle-*`, `syntax-*`.
- Page `#ffffff` (use bone `--cds-background`).
- Coral `#f15b40` as small/body text or helper. Small primary: ink or ghost.
- Dump coral onto all `--cds-interactive` consumers.
- Raw hex / raw px in screen-spec or Storybook twin when a token exists.
- Invent a component missing from `component-token-map.json`.
- `figma-generate-library` on this Community kit.
- Figma Make, Claude Artifacts, Lovable, v0, shadcn, Tailwind-as-DS for Luma
  screens.
- Change Figma default variable mode away from IBM.
- Copy CoForge `token-keeper`, ux-atlas `DESIGN.md` / `styleguide`, or Gate B
  Python into this repo.
- Mix `code-connect-parserless` edits into a screen commit.
- User quotes that were not given. Ungrounded needs: `ASSUMPTION`.
