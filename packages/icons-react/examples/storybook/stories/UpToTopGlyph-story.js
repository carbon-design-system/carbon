import React from 'react';
import { storiesOf } from '@storybook/react';
import UpToTopGlyph from '../../../lib/up-to-top/glyph';

storiesOf('UpToTopGlyph', module).add('default', () => <UpToTopGlyph />);
