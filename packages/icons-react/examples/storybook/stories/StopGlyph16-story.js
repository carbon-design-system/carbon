import React from 'react';
import { storiesOf } from '@storybook/react';
import StopGlyph16 from '../../../lib/stop--glyph/16';

storiesOf('StopGlyph16', module).add('default', () => <StopGlyph16 />);
