import React from 'react';
import { storiesOf } from '@storybook/react';
import CheckmarkGlyph from '../../../lib/checkmark/glyph';

storiesOf('CheckmarkGlyph', module).add('default', () => <CheckmarkGlyph />);
