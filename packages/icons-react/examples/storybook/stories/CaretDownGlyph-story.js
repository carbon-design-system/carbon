import React from 'react';
import { storiesOf } from '@storybook/react';
import CaretDownGlyph from '../../../lib/caret--down/glyph';

storiesOf('CaretDownGlyph', module).add('default', () => <CaretDownGlyph />);
