import React from 'react';
import { storiesOf } from '@storybook/react';
import CheckboxChecked32 from '../../../lib/checkbox--checked/32';

storiesOf('CheckboxChecked32', module).add('default', () => (
  <CheckboxChecked32 />
));
