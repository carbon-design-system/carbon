import React from 'react';
import { storiesOf } from '@storybook/react';
import ErrorGlyph16 from '../../../lib/error--glyph/16';

storiesOf('ErrorGlyph16', module).add('default', () => <ErrorGlyph16 />);
