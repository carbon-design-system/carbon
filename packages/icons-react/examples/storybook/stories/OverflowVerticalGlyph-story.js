import React from 'react';
import { storiesOf } from '@storybook/react';
import OverflowVerticalGlyph from '../../../lib/overflow--vertical/glyph';

storiesOf('OverflowVerticalGlyph', module).add('default', () => (
  <OverflowVerticalGlyph />
));
