import React from 'react';
import { storiesOf } from '@storybook/react';
import CaretLeftGlyph from '../../../lib/caret--left/glyph';

storiesOf('CaretLeftGlyph', module).add('default', () => <CaretLeftGlyph />);
