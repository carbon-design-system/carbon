import React from 'react';
import { storiesOf } from '@storybook/react';
import DraggableGlyph from '../../../lib/draggable/glyph';

storiesOf('DraggableGlyph', module).add('default', () => <DraggableGlyph />);
