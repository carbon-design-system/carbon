import React from 'react';
import { storiesOf } from '@storybook/react';
import ChevronDownGlyph from '../../../lib/chevron--down/glyph';

storiesOf('ChevronDownGlyph', module).add('default', () => (
  <ChevronDownGlyph />
));
