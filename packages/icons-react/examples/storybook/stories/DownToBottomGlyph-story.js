import React from 'react';
import { storiesOf } from '@storybook/react';
import DownToBottomGlyph from '../../../lib/down-to-bottom/glyph';

storiesOf('DownToBottomGlyph', module).add('default', () => (
  <DownToBottomGlyph />
));
