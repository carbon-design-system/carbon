import React from 'react';
import { storiesOf } from '@storybook/react';
import OverflowMenuGlyph from '../../../lib/overflow-menu/glyph';

storiesOf('OverflowMenuGlyph', module).add('default', () => (
  <OverflowMenuGlyph />
));
