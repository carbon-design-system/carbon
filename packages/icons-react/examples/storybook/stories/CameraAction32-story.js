import React from 'react';
import { storiesOf } from '@storybook/react';
import CameraAction32 from '../../../lib/camera--action/32';

storiesOf('CameraAction32', module).add('default', () => <CameraAction32 />);
