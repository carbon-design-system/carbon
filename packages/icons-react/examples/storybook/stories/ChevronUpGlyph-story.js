import React from 'react';
import { storiesOf } from '@storybook/react';
import ChevronUpGlyph from '../../../lib/chevron--up/glyph';

storiesOf('ChevronUpGlyph', module).add('default', () => <ChevronUpGlyph />);
