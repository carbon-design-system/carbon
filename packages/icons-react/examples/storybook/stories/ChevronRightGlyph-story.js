import React from 'react';
import { storiesOf } from '@storybook/react';
import ChevronRightGlyph from '../../../lib/chevron--right/glyph';

storiesOf('ChevronRightGlyph', module).add('default', () => (
  <ChevronRightGlyph />
));
