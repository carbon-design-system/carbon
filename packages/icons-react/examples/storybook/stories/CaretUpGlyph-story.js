import React from 'react';
import { storiesOf } from '@storybook/react';
import CaretUpGlyph from '../../../lib/caret--up/glyph';

storiesOf('CaretUpGlyph', module).add('default', () => <CaretUpGlyph />);
