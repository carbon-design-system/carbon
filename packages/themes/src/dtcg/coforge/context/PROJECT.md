# Project

**Product workstream:** Luma Travel (synthetic corpus; Define-phase draft; Gate
A unsigned). Load from sibling CoForge OS via [`rag/RAG.md`](../rag/RAG.md) — do
not copy that repo here. **Implementation surface:** this Carbon-DS monorepo,
CoForge overlay on IBM Carbon. **Git:** branch `Coforge_skin` only. Do not merge
to `main` unless asked. **Surfaces:** `@carbon/react` Storybook (toolbar CoForge
→ CoForge skin) and `@carbon/web-components`. Dual flagship — same composition,
vendor names. **Figma:** Carbon Community v11 branch `mnPFHuLUzXItWQimrWQEvV`.
IBM default variable mode. Overlay bindings only where CSS already proves the
role. **Not in this project:** a fifth IBM theme, dark skin (`g90`/`g100`),
radius language, invented L2 CoForge components, nesting CoForge OS inside this
repo.
