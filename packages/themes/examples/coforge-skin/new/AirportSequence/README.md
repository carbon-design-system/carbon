# AirportSequence

Physical places in order: documents → TSA (link-out) → passport vs immigration →
gate.

**Why new:** `ProgressIndicator` means “finish the task” (usually pay). P12
sequence **is** the product. N2.

**Wraps:** `OrderedList` or stacked `Tile`s, `Link` (TSA), `Button` ghost for
next/back  
**Forbidden:** agency Book; MyTSA clone of checkpoint contents; “simplified”
onboarding chrome
