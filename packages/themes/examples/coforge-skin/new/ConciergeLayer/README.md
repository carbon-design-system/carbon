# ConciergeLayer

Header global action + panel. Routes into jobs and `/airport`. Never Book.

**Why new:** `Switcher` is IBM product-switch. N2.

**Wraps:** `HeaderGlobalAction`, `HeaderPanel`, `Link` or `Button` ghost  
**Forbidden:** Book, plan-for-me, seat sale
