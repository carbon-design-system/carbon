# DocumentPair

Outbound vs return as two documents: position, size, label, confirmation. Wrong
blocked.

**Why new:** `RadioButtonGroup` equalizes choices. Anti-HMW #4. ART-039 Bernard
unowned.

**Wraps:** `Form`, `Button` lg, `InlineNotification`; inner structure may use
`Tile` **as the selectable document**, not a separate radio column  
**Forbidden:** print both legs as one control; sell seat; age-coded “simple” UI
