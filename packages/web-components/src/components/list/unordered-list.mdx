import { ArgTypes, Canvas, Markdown, Meta } from '@storybook/addon-docs/blocks';
import { cdnJs } from '../../globals/internal/storybook-cdn';
import * as UnorderedListStories from './unordered-list.stories';

<Meta of={UnorderedListStories} />

# Unordered list

[Source code](https://github.com/carbon-design-system/carbon/tree/main/packages/web-components/src/components/list)
&nbsp;|&nbsp;
[Usage guidelines](https://www.carbondesignsystem.com/components/list/usage)
&nbsp;|&nbsp;
[Accessibility](https://www.carbondesignsystem.com/components/list/accessibility)

## Table of Contents

- [Overview](#overview)
- [Marker Types](#marker-types)
- [Nested](#Nested)
- [Component API](#component-api)
- [CDN](#cdn)
- [Feedback](#feedback)

## Overview

<Canvas of={UnorderedListStories.Default} />

## Marker Types

The `cds-unordered-list` component supports different marker types for list items. You can specify the marker type using the `type` attribute:

- **`disc`**: Filled circle (•) - uses native CSS `list-style-type: disc`
- **`circle`**: Hollow circle (○) - uses native CSS `list-style-type: circle`
- **`square`**: Filled square (▪) - uses native CSS `list-style-type: square`
- **`hyphen`**: En dash (–) - uses a custom `::before` pseudo-element (default for top-level lists)
- **`custom`**: Custom marker - uses a custom `::before` pseudo-element with content from the `custom-marker` attribute

When no `type` is specified:
- Top-level lists default to `hyphen`
- Nested lists default to `square` (deprecated - will inherit parent type in next major release)

<Canvas of={UnorderedListStories.MarkerTypes} />

### Nested Lists with Marker Types

You can specify different marker types for nested lists. In the next major release, nested lists without an explicit `type` will inherit the parent list's marker type.

<Canvas of={UnorderedListStories.NestedWithMarkerTypes} />

## Nested

<Canvas of={UnorderedListStories.Nested} />

## Component API

## `cds-unordered-list`

<ArgTypes of="cds-unordered-list" />

## `cds-list-item`

<ArgTypes of="cds-list-item" />

<Markdown>{`${cdnJs({ components: ['list'] })}`}</Markdown>

## Feedback

Help us improve this component by providing feedback, asking questions on Slack,
or updating this file on
[GitHub](https://github.com/carbon-design-system/carbon/edit/main/packages/web-components/src/components/list/unordered-list.mdx).
