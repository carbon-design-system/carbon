import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import * as EditInPlaceStories from './edit-in-place.stories';

<Meta of={EditInPlaceStories} />

# Edit in place

The EditInPlace component allows users to edit text inline with save and cancel
actions. It provides a seamless editing experience with keyboard support and
validation.

## Getting started

Here's a quick example to get you started.

### JS (via import)

```javascript
import '@carbon/web-components/es/components/edit-in-place/index.js';
```

### HTML

```html
<cds-edit-in-place
  id="my-edit"
  value="Edit me"
  label-text="Editable field"
  edit-label="Edit"
  save-label="Save"
  cancel-label="Cancel"></cds-edit-in-place>
```

### JavaScript (Event Handling)

```javascript
const editInPlace = document.querySelector('cds-edit-in-place');

editInPlace.addEventListener('cds-edit-in-place-change', (e) => {
  console.log('Value changed:', e.detail.value);
});

editInPlace.addEventListener('cds-edit-in-place-save', (e) => {
  console.log('Saved:', e.detail.value);
});

editInPlace.addEventListener('cds-edit-in-place-cancel', (e) => {
  console.log('Cancelled:', e.detail.value);
});
```

### Example Usage

### Default

<Canvas of={EditInPlaceStories.Default} />

### Invalid

<Canvas of={EditInPlaceStories.Invalid} />

### Custom Blur Function

<Canvas of={EditInPlaceStories.CustomBlurFunction} />

### Read Only

<Canvas of={EditInPlaceStories.ReadOnly} />

## Component API

### Properties

| Property                | Attribute                  | Type                   | Default                        | Description                                     |
| ----------------------- | -------------------------- | ---------------------- | ------------------------------ | ----------------------------------------------- |
| `cancelLabel`           | `cancel-label`             | `string`               | `'Cancel'`                     | Label for the cancel button                     |
| `editAlwaysVisible`     | `edit-always-visible`      | `boolean`              | `false`                        | Always show the edit icon (not just on hover)   |
| `editLabel`             | `edit-label`               | `string`               | `'Edit'`                       | Label for the edit button                       |
| `id`                    | `id`                       | `string`               | `''`                           | Specify a custom id for the input               |
| `inheritTypography`     | `inherit-typography`       | `boolean`              | `false`                        | Inherit typography from container               |
| `invalid`               | `invalid`                  | `boolean`              | `false`                        | Determines if the input is invalid              |
| `invalidText`           | `invalid-text`             | `string`               | `''`                           | Text displayed when input is invalid            |
| `labelText`             | `label-text`               | `string`               | `''`                           | Text for screen readers                         |
| `placeholder`           | `placeholder`              | `string`               | `''`                           | Placeholder text for the input                  |
| `readOnly`              | `read-only`                | `boolean`              | `false`                        | Determines if the input is in read-only mode    |
| `readOnlyLabel`         | `read-only-label`          | `string`               | `'Edit off'`                   | Label for the edit off button in read-only mode |
| `readOnlyToggleTipText` | `read-only-toggletip-text` | `string`               | `'This field is read-only...'` | Text for the tooltip shown in read-only mode    |
| `saveLabel`             | `save-label`               | `string`               | `'Save'`                       | Label for the save button                       |
| `size`                  | `size`                     | `'sm' \| 'md' \| 'lg'` | `'sm'`                         | Vertical size of control                        |
| `toggleTipAlignment`    | `toggletip-alignment`      | `TooltipAlignment`     | `'bottom'`                     | Tooltip alignment in read-only mode             |
| `tooltipAlignment`      | `tooltip-alignment`        | `TooltipAlignment`     | `'top'`                        | Tooltip alignment for buttons                   |
| `value`                 | `value`                    | `string`               | `''`                           | Current value of the input                      |

### TooltipAlignment Type

```typescript
type TooltipAlignment =
  | 'top'
  | 'top-left'
  | 'top-right'
  | 'bottom'
  | 'bottom-left'
  | 'bottom-right'
  | 'left'
  | 'right';
```

## Events

| Event                      | Description                                                 | Detail              |
| -------------------------- | ----------------------------------------------------------- | ------------------- |
| `cds-edit-in-place-change` | Fired when the input value changes                          | `{ value: string }` |
| `cds-edit-in-place-save`   | Fired when save is triggered (button click or Enter key)    | `{ value: string }` |
| `cds-edit-in-place-cancel` | Fired when cancel is triggered (button click or Escape key) | `{ value: string }` |
| `cds-edit-in-place-blur`   | Fired when the input loses focus                            | `{ value: string }` |

## CSS Parts

The component exposes the following CSS parts for external styling:

| Part           | Description                 |
| -------------- | --------------------------- |
| `input`        | The input element           |
| `actions`      | The button container        |
| `invalid-text` | The error message container |

### Example: Styling with CSS Parts

```css
cds-edit-in-place::part(input) {
  font-weight: bold;
}

cds-edit-in-place::part(actions) {
  background-color: lightgray;
}

cds-edit-in-place::part(invalid-text) {
  font-style: italic;
}
```

## Accessibility

- The component uses proper ARIA attributes for screen readers
- Keyboard navigation is fully supported:
  - **Enter**: Save changes (when in edit mode)
  - **Escape**: Cancel changes (when in edit mode)
  - **Tab**: Navigate between input and action buttons
- All buttons have accessible labels via tooltips
- Invalid state is properly announced to screen readers

## Feedback

Help us improve this component by providing feedback, asking questions on Slack,
or updating this file in
[GitHub](https://github.com/carbon-design-system/carbon/tree/main/packages/web-components/src/components/edit-in-place/edit-in-place.mdx).
