// DEMO
body,
.demo-container {
  background-color: $color__gray-3;
}

.left-nav__header {
  display: flex;
  align-items: center;
  height: 100%;
  transition: .3s ease-in-out;
  border-bottom: 1px solid transparent;
  background-color: $color__white;
  width: rem(55px);

  &--title {
    @include font-size('18');
    color: $color__blue-51;
    opacity: 0;
  }

  .left-nav--active & {
    width: rem(250px);
  }
}

.left-nav__trigger {
  width: rem(55px);
  height: rem(50px);
  background-color: $color__navy-gray-3;
  border-right: 1px solid transparent;

  .left-nav--active & {
    background-color: $color__white;
    border-right: 1px solid $color__gray-1;
  }

  &--btn {
    padding: 1.525rem;
    position: relative;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;

    span,
    span:before,
    span:after {
      height: rem(2px);
      background: $color__blue-40;
      cursor: pointer;
      position: absolute;
      display: block;
      content: '';
      transition: all $transition--base ease;
    }

    span {
      width: .75rem;
    }

    span:before {
      width: 1rem;
    }

    span:after {
      width: .85rem;
    }

    span:before {
      top: rem(-5px);
    }

    span:after {
      bottom: rem(-5px);
    }

    .bx--global-header__left-nav.left-nav--active & span {
      background-color: transparent;
    }

    .bx--global-header__left-nav.left-nav--active & span:before,
    .bx--global-header__left-nav.left-nav--active & span:after {
      top: 0;
      background-color: $color__blue-51;
    }

    .bx--global-header__left-nav.left-nav--active & span:before {
      transform: rotate(45deg);
      width: 1rem;
    }

    .bx--global-header__left-nav.left-nav--active & span:after {
      transform: rotate(-45deg);
      width: 1rem;
    }
  }
}

.left-nav {
  display: block;
  width: rem(250px);
  height: 100vh;
  top: rem(90px);
  left: 0;
  position: fixed;
  background-color: $color__white;
  padding: .75rem 0 2rem 3.5rem;
  transform: translateX(-100%);
  transition: .3s ease-in-out;
  z-index: z('overlay');
  box-shadow: none;
  border-top: 1px solid transparent;

  & > .left-nav-list {
    opacity: 0;
  }

  @include breakpoint('bp--md--major') {
    padding-left: 1rem;
  }

  .left-nav--active & {
    box-shadow: 4px 4px 4px 0 rgba(0, 0, 0, .1);
    transform: translateX(0);
    border-top: 1px solid $color__gray-1;

    & > .left-nav-list {
      opacity: 1;
    }
  }
}

.left-nav-list {
  display: flex;
  flex-direction: column;

  &__item {
    cursor: pointer;
    width: 100%;

    &--active {

      & > .left-nav-list__item-link {
        color: $color__blue-51;
        font-weight: 400;
      }
    }

    &-link {
      @include font-size('16');
      // @include font-smoothing;
      position: relative;
      display: block;
      color: $color__blue-90;
      text-decoration: none;
      padding: .15rem 0 .15rem .5rem;
      margin: .25rem 0;
      font-weight: 400;
      transition: $transition--base;

      &:hover {
        background-color: $color__blue-51;
        color: $color__white;
      }
    }

    // &-icon {
    //
    //   .icon {
    //     display: inline-block;
    //     position: absolute;
    //     width: rem(12px);
    //     height: rem(12px);
    //     left: rem(-32px);
    //     top: rem(14px);
    //     opacity: 1;
    //     fill: $color__navy-gray-7;
    //     transform: rotate(-90deg);
    //     transition: $transition--base;
    //   }
    //
    //   &--active {
    //
    //     .icon {
    //       fill: $color__blue-30;
    //     }
    //   }
    // }

    &--expanded {

      .left-nav-list__item-link {
        font-weight: 500;
      }

      .left-nav-list--nested {
        max-height: 20rem;
        transition: .5s;
        overflow: inherit;
      }

      .left-nav-list__item-icon .icon {
        transform: rotate(0);
        fill: $color__blue-30;
      }
    }
  }
}

.left-nav-list--nested {
  max-height: 0;
  overflow: hidden;
  transition: $transition--base;

  .left-nav-list__item {
    width: 100%;
    margin: .25rem 0;
    position: relative;

    &-link {
      @include font-size('14');
      color: $color__navy-gray-3;
      padding: .35rem 1rem .35rem .5rem;
      transition: $transition--base;
      font-weight: 400;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    &--active {

      & > .left-nav-list__item-link {
        font-weight: 400;

        background-color: $color__blue-51;
        color: $color__white;

        .left-nav-list--flyout--arrow-icon {
          fill: $color__white;
        }

        &:hover {
          background-color: $color__blue-51;
          color: $color__white;
        }
      }
    }
  }
}

.left-nav-list--flyout {
  @include box-shadow;
  position: absolute;
  background-color: $color__white;
  z-index: 9000;
  top: rem(-8px);
  padding: .5rem 0;
  display: none;
  left: 168px;
  min-width: auto;
  white-space: nowrap;

  &--arrow {

    &-icon {
      width: 12px;
      height: 12px;
      fill: $color__blue-90;
    }
  }

  &--displayed {
    display: block;
  }

  .left-nav-list__item {
    padding: .1rem 1rem 0rem .2rem;
    margin: 0;
    transition: .2s;

    &-link {
      @include font-size('12');
    }

    &:hover {
      background-color: $color__blue-51;

      .left-nav-list__item-link {
        color: $color__white;
      }
    }
  }
}

.sidebar-toggle {
  display: block;
  width: rem(50px);
  position: relative;
  cursor: pointer;
  padding: 1.5rem 5%;

  @include breakpoint(bp--sm--major) {
    display: none;
  }

  span,
  span:before,
  span:after {
    height: rem(3px);
    width: rem(30px);
    background: $color__navy-gray-7;
    cursor: pointer;
    position: absolute;
    display: block;
    content: '';
    transition: all $transition--base ease-in-out;
  }

  span:before {
    top: rem(-9px);
  }

  span:after {
    bottom: rem(-9px);
  }
}

.bx--global-header__left-nav.left-nav--active {

  .bx--dropdown {
    width: rem(195px);
  }

  .bx--dropdown--selected {
    display: inherit;
  }

  .bx--dropdown__menu-arrow {
    opacity: 1;
    transition: opacity $transition--base ease $transition--base, transform $transition--base ease-in-out 0s;
  }

  .bx--dropdown__menu-text {
    background-color: $color__white;
    color: $color__blue-40;

    &:hover {
      background-color: $color__blue-51;
      color: $color__white;

      & + .bx--dropdown__menu-arrow {
        fill: $color__white;
      }
    }
  }

  .bx--dropdown__list {
    background-color: $color__white;
    color: $color__blue-90;
    border-top: 1px solid $color__gray-1;
    border-left: 1px solid $color__gray-1;
    border-bottom: 1px solid $color__gray-1;
  }
}

// DEMO CONTAINER

.demo--container {
  padding: 0 !important;
  display: flex;
}
